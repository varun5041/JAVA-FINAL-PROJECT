# Database Configuration
# Modify these values according to your MySQL setup
# IMPORTANT: Make sure you have created the database in MySQL Workbench first!

# Connection URL
# Format: jdbc:mysql://[host]:[port]/[database_name]?[options]
# If you created the database with a different name, change 'library_db' below
db.url=jdbc:mysql://127.0.0.1:3306/library_db?useSSL=false&allowPublicKeyRetrieval=true

# MySQL Username (usually 'root')
db.username=root

# MySQL Password (leave empty if no password, otherwise enter your MySQL root password)
# If you set a password in MySQL Workbench, enter it here
db.password=Varun5041@

# Instructions:
# 1. Open MySQL Workbench and connect to your MySQL server
# 2. Run the database_schema.sql script to create the database and tables
# 3. Update the db.password above if you have a MySQL password set
# 4. The database will appear in MySQL Workbench Navigator panel after creation
